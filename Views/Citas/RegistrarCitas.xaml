<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoP2.RegistrarCitas"
             Title="RegistrarCitas"
             BackgroundColor="Wheat">
    <VerticalStackLayout Padding="20">
        <DatePicker x:Name="datePicker" Date="{Binding NuevaCita.FechaHora, Mode=TwoWay}" />
        <TimePicker x:Name="timePicker" Time="{Binding NuevaCita.FechaHora, Mode=TwoWay}" />
        <Entry Placeholder="Motivo" Text="{Binding Motivo}" />
        <Entry Placeholder="Nombre del Animal" Text="{Binding NombreAnimal}" />
        <Entry Placeholder="Nombre del Propietario" Text="{Binding NombrePropietario}" />
        <Entry Placeholder="Teléfono del Propietario" Text="{Binding NumeroTelefonoPropietario}" />
        <Editor Placeholder="Observaciones" Text="{Binding Observaciones}" />

        <Button Text="Registrar" Clicked="RegistrarClicked" />
    </VerticalStackLayout>
</ContentPage>